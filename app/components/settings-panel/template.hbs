
<h3 class="no-top-gap">{{t 'settings.title'}}</h3>

<label for="current-locale">{{t 'settings.language'}}</label>

{{#blackout-select
  selected=currentLocale
  onchange=(action "changeLocale")
  options=locales as |locale|
}}
{{locale.label}}
{{/blackout-select}}

