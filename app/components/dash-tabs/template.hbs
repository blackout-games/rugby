 

{{blackout-tabs-slider tabPanels=dashTabs isShowing=isShowing selectedTab=(readonly selectedTab) onTabSelect=(action 'tabSelected') debug=debug }}

{{#if hasHeader}}
  {{yield 'header'}}
{{/if}}

{{#blackout-tabs tabPanels=(mut dashTabs) selectedTab=(readonly currentTab) onTabChange=(action 'tabChanged') hideTabs=true tabGroup=tabGroup tabRoute=tabRoute isShowing=isShowing }}
  
  {{yield}}
  
{{/blackout-tabs}}
