<h5 class="gap-2x no-top-gap">{{t 'player.manage.actions'}}</h5>

<div class="gap-md">

  {{#confirm-button class="btn-big btn-right-gap btn-red" clickAction=(action 'firePlayer')}}{{t 'buttons.fire-player'}}{{/confirm-button}}
  
  {{! other actions here }}
  
</div>

{{#if actionsError}}
<div class="has-error">
  <div class="control-label">{{actionsError}}</div>
</div>
{{/if}}