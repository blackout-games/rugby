{{#if inline}}
  
  {{#if stacked}}
    
    <div class="inline-block">
      <div class="text-center">
        {{user-image type='club' defaultColor=defaultColor club=club size=imageSize}}
      </div>
      <div>
        <span>{{if text text club.name}}</span>
      </div>
    </div>
    
  {{else}}
    
    <span class="nowrap">
      {{user-image type='club' defaultColor=defaultColor club=club size='small' class="club-avatar-inline no-right-gap left-gap-xs"}}
      <span class="btn-a-hover">{{if text text club.name}}</span>
    </span>
    
  {{/if}}
  
{{else}}
    
  <div class="{{unless stacked "row row-nowrap"}}">
    <div class="{{unless stacked "col-auto right-gap-md"}} text-center">
      {{user-image type='club' defaultColor=defaultColor club=club size=imageSize}}
    </div>
    <div class="{{unless stacked "col-auto vc-parent"}}">
      <span>{{if text text club.name}}</span>
    </div>
  </div>
  
{{/if}}