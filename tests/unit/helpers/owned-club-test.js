import { ownedClub } from 'rugby-ember/helpers/owned-club';
import { module, test } from 'qunit';

module('Unit | Helper | owned club');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = ownedClub([42]);
  assert.ok(result);
});
