import { last } from 'rugby-ember/helpers/last';
import { module, test } from 'qunit';

module('Unit | Helper | last');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = last([42]);
  assert.ok(result);
});
