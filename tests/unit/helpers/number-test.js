import { number } from '../../../helpers/number';
import { module, test } from 'qunit';

module('Unit | Helper | number');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = number(42);
  assert.ok(result);
});
