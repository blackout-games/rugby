import { manager } from '../../../helpers/manager';
import { module, test } from 'qunit';

module('Unit | Helper | manager');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = manager(42);
  assert.ok(result);
});
