import { competition } from 'rugby-ember/helpers/competition';
import { module, test } from 'qunit';

module('Unit | Helper | competition');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = competition([42]);
  assert.ok(result);
});
