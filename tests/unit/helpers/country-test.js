import { country } from '../../../helpers/country';
import { module, test } from 'qunit';

module('Unit | Helper | country');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = country(42);
  assert.ok(result);
});
