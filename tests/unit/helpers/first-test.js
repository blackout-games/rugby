import { first } from 'rugby-ember/helpers/first';
import { module, test } from 'qunit';

module('Unit | Helper | first');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = first([42]);
  assert.ok(result);
});
