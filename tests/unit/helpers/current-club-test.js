import { currentClub } from 'rugby-ember/helpers/current-club';
import { module, test } from 'qunit';

module('Unit | Helper | current club');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = currentClub([42]);
  assert.ok(result);
});
