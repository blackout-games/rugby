import { geti } from 'rugby-ember/helpers/geti';
import { module, test } from 'qunit';

module('Unit | Helper | geti');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = geti([42]);
  assert.ok(result);
});
