import { safe } from 'rugby-ember/helpers/safe';
import { module, test } from 'qunit';

module('Unit | Helper | safe');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = safe([42]);
  assert.ok(result);
});
