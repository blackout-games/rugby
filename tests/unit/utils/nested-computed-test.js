import nestedComputed from 'rugby-ember/utils/nested-computed';
import { module, test } from 'qunit';

module('Unit | Utility | nested computed');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = nestedComputed();
  assert.ok(result);
});
