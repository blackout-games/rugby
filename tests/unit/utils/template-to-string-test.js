import templateToString from '../../../utils/template-to-string';
import { module, test } from 'qunit';

module('Unit | Utility | template to string');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = templateToString();
  assert.ok(result);
});
