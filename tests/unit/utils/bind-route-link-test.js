import bindRouteLink from '../../../utils/bind-route-link';
import { module, test } from 'qunit';

module('Unit | Utility | bind route link');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = bindRouteLink();
  assert.ok(result);
});
